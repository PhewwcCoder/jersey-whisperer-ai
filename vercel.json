{
  "devCommand": "vite --port $PORT --configLoader runner",
  "buildCommand": "vite build --configLoader runner",
  "outputDirectory": "dist/client",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
